# Gautam Kumar Mahar 2103114
# Lab 3 - Third

.text 
main :
      lb $t1, 5($zero)         # $zero register contains the address 0, so 5($zero) means the memory address 5.
                               # lb instruction loads a single byte from memory at the address 5 into register $t1.

 